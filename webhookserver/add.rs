// This program adds two numbers and prints the result
fn main() {
    // Define the numbers to be added
    let a = 4;
    let b = 5;

    // Calculate the sum
    let sum = a + b;

    // Print the result
    println!("{}", sum);
}
